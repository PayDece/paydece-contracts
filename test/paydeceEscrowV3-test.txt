// const { expect } = require("chai");
// const { ethers, providers } = require("hardhat");


// //setTimeProcess
// describe("setTimeProcess", () => {
//   it("should revert with message 'The timeProcess can be >= 0", async () => {
//     let PaydeceEscrow, paydeceEscrow;
//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // await expect(paydeceEscrow.setTimeProcess(10)).to.be.revertedWith(
//     //   "The timeProcess can be >= 0"
//     // );

//     await expect(
//       paydeceEscrow.connect(owner).setTimeProcess(0)
//     ).to.be.revertedWith("The timeProcess must be greater than 0");

//     await paydeceEscrow.setTimeProcess(1);

//     // expect(Number(feeSender)).to.equal(Number(1));
//   });
// });

// describe("addStablesAddresses & delStablesAddresses", () => {
//   it("you should add and remove the EC20 stable addresses", async () => {
//     let PaydeceEscrow, paydeceEscrow;
//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     const usdtAddress = "0x55d398326f99059fF775485246999027B3197955";
//     await paydeceEscrow.addStablesAddresses(usdtAddress);
//     await paydeceEscrow.delStablesAddresses(usdtAddress);

//     const usdcAddress = "0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d";
//     await paydeceEscrow.addStablesAddresses(usdcAddress);
//     await paydeceEscrow.delStablesAddresses(usdcAddress);

//     //delStablesAddresses Error
//     await expect(
//       paydeceEscrow.connect(other).addStablesAddresses(usdcAddress)
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(other).delStablesAddresses(usdcAddress)
//     ).to.be.revertedWith("Ownable: caller is not the owner");
//   });
// });

// describe("PaydeceEscrow StableCoin", function () {
//   //StableCoin
//   it("should create a escrow and release StableCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     const _amountFeeSender =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     // ).to.be.revertedWith("reverted with reason string 'ERC20: transfer amount exceeds allowance'");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, true, true);

//     await expect(
//       paydeceEscrow.connect(Receiver).releaseEscrow("1")
//     ).to.be.revertedWith("Status must be FIATCOIN_TRANSFERED");

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");
    
//     //call setMarkAsPaid
//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call releaseEscrow
//     await paydeceEscrow.connect(Receiver).releaseEscrow("1");

//     // const __state = await paydeceEscrow.connect(owner).getState("1");
//     //  console.log("stata:",__state);    

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("scBalance:" , scBalance);
//     expect(Number(scBalance)).to.equal(Number(0));

//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     // console.log("ReceiverBalance:" , ReceiverBalance.toString());
//     expect(Number(receiverBalance)).to.equal(Number(0));

//     const senderBalance = await usdt.balanceOf(Sender.address);
//     // console.log("SenderBalance:" , SenderBalance.toString());
//     expect(Number(senderBalance)).to.equal(Number(100 * 10 ** decimals));
//   });

//   it("should releaseEscrowOwner to Sender StableCoin 1", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFee = (ammount * (500 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     //check that it is not possible to create without having a stable currency in the list.
//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Address Stable to be whitelisted");
    
//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Sender.address, ammount + _amountFee);
    
//     //call approve
//     await usdt
//       .connect(Sender)
//       .approve(paydeceEscrow.address, ammount + _amountFee);
    
//     //check that it is not possible to create an escrow with the same sender and receiver.  
//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");
    
//     //call approve
//     await usdt
//       .connect(Sender)
//       .approve(paydeceEscrow.address, ammount + _amountFee);
    
//     //call createEscrow
//     await paydeceEscrow
//       .connect(Sender)
//       .createEscrow("1", Receiver.address, ammount, usdt.address, false, false);
    
//     //Check that it is not possible to create an escrow with the same id.  
//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Receiver.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");
    
//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");
    
//     //call setMarkAsPaid
//     await paydeceEscrow.connect(Receiver).setMarkAsPaid("1");
    
//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");
    
//     //get Balance
//     // const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // expect(Number(scBalance)).to.equal(Number(0));
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     expect(Number(receiverBalance)).to.equal(Number(99500000)); // 100-0.5%=99.5
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });

//   it("should refund to Receiver StableCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeSender =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;      

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeSender);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(
//         paydeceEscrow.address,
//         ammount + _amountFeeSender
//       );

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     // console.log("Paso1");
//     // const _feeAvaible = await paydeceEscrow.connect(Receiver).feesAvailable(usdt.address);  
//     // console.log("_feeAvaible",_feeAvaible.toString());
    
//     //call refundReceiver Error
//     await expect(
//       paydeceEscrow.connect(other).refundSender("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call refundReceiver
//     await paydeceEscrow.connect(owner).refundSender("1");

//   //call refundReceiver
//   await expect(
//     paydeceEscrow.connect(owner).refundSender("1")
//   ).to.be.revertedWith("Refund not approved");    
  
//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("==========scBalance:" + scBalance);
//     expect(Number(scBalance)).to.equal(Number(0));
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     // console.log("==========ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(
//       Number(100 * 10 ** decimals) + 1000000
//     );
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     // console.log("==========SenderBalance:" + SenderBalance);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });

//   it("should feesAvailable & withdrawFees StableCoin 1", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     //call approve
//     await usdt.connect(Receiver).approve(paydeceEscrow.address, ammount);

//     await expect(
//       paydeceEscrow.connect(other).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(owner).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Amount > feesAvailable");

//     //call createEscrow zero
//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, 0, usdt.address, false, false)
//     ).to.be.revertedWith("The parameter value cannot be zero");

//     //call createEscrow Receiver zero
//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow(
//           "2",
//           ethers.constants.AddressZero,
//           ammount,
//           usdt.address,
//           false,
//           false
//         )
//     ).to.be.revertedWith("The address receiver cannot be empty");

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");

//     const _feesAvailable = await paydeceEscrow
//       .connect(owner)
//       .feesAvailable(usdt.address);
//     // console.log("feesAvailable:" + _feesAvailable);

//     const scBalance = await usdt.balanceOf(owner.address);

//     // console.log("scBalance:" + scBalance);
//     //withdrawFees
//     const _releaseEscrowOwner = await paydeceEscrow
//       .connect(owner)
//       .withdrawFees(usdt.address);

//     //get Balance
//     const scAfterBalance = await usdt.balanceOf(owner.address);
//     // console.log("scAfterBalance:" + scAfterBalance);

//     expect(Number(scAfterBalance)).to.equal(
//       Number(scBalance) + Number(_feesAvailable)
//     );
//   });

//   //CancelReceiver
//   it("should create a escrow and CancelReceiver", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     const _amountFeeSender =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     // ).to.be.revertedWith("Receiver approve to Escrow first");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, true, true);

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     await expect(
//       paydeceEscrow.connect(other).cancelReceiver("1")
//     ).to.be.revertedWith("Only Receiver can call this");

//     await paydeceEscrow.connect(Sender).cancelReceiver("1");

//     await expect(
//       paydeceEscrow.connect(Sender).cancelReceiver("1")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     //console.log("scBalance:" + scBalance);
//     expect(Number(scBalance)).to.equal(Number(0));

//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     //console.log("ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(Number(100 * 10 ** decimals));

//     const senderBalance = await usdt.balanceOf(Sender.address);
//     //console.log("SenderBalance:" + SenderBalance);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });

//   //CancelSender
//   it("should create a escrow and CancelSender", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     const _amountFeeSender =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     // ).to.be.revertedWith("Receiver approve to Escrow first");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, true, true);

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     await paydeceEscrow.connect(owner).setTimeProcess(5000);

//     await expect(
//       paydeceEscrow.connect(Sender).cancelSender("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     await expect(
//       paydeceEscrow.connect(Receiver).cancelSender("1")
//     ).to.be.revertedWith("Time is still running out.");

//     await paydeceEscrow.connect(owner).setTimeProcess(1);

//     await paydeceEscrow.connect(Receiver).cancelSender("1");

//     await expect(
//       paydeceEscrow.connect(Receiver).cancelSender("1")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("scBalance:" , scBalance.toString());
//     expect(Number(scBalance)).to.equal(Number(0));

//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     //console.log("ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(Number(100 * 10 ** decimals));

//     const senderBalance = await usdt.balanceOf(Sender.address);
//     //console.log("SenderBalance:" + SenderBalance);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });
// });

// describe("PaydeceEscrow StableCoin setMarkAsPaidOwner", function () {
//   //StableCoin
//   it("should create a escrow and release StableCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     const _amountFeeSender =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     // ).to.be.revertedWith("Receiver approve to Escrow first");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, true, true);

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     // await paydeceEscrow.connect(other).setMarkAsPaidOwner("1");
//     await expect(
//       paydeceEscrow.connect(other).setMarkAsPaidOwner("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call setMarkAsPaid
//     await paydeceEscrow.connect(owner).setMarkAsPaidOwner("1");

//     //call releaseEscrow
//     await paydeceEscrow.connect(Receiver).releaseEscrow("1");

//     await expect(
//       paydeceEscrow.connect(owner).setMarkAsPaidOwner("1")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("scBalance:" , scBalance.toString());
//     expect(Number(scBalance)).to.equal(Number(0));
    
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     //console.log("ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(Number(0));
    
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     //console.log("senderBalance:" + senderBalance);
//     expect(Number(senderBalance)).to.equal(Number(100 * 10 ** decimals));
    
//   });

//   it("should releaseEscrowOwner to Sender StableCoin 2", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;
    
//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     // ).to.be.revertedWith("Receiver approve to Escrow first");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     //call setMarkAsPaid
//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");


//     await expect(
//       paydeceEscrow.connect(other).releaseEscrowOwner("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");
    
//     //get Balance
//     // const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // expect(Number(scBalance)).to.equal(Number(1 * 10 ** decimals));

//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     expect(Number(receiverBalance)).to.equal(Number(0));
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     expect(Number(senderBalance)).to.equal(Number(100 * 10 ** decimals));
//   });

//   it("should refund to Receiver StableCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(
//         paydeceEscrow.address,
//         ammount + _amountFeeReceiver + _amountFeeReceiver
//       );

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     //call refundReceiver Error
//     await expect(
//       paydeceEscrow.connect(other).refundSender("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call refundReceiver
//     await paydeceEscrow.connect(owner).refundSender("1");

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("==========scBalance:" + scBalance);
//     expect(Number(scBalance)).to.equal(Number(0));
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     // console.log("==========ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(
//       Number(100 * 10 ** decimals) + 1000000
//     );
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     // console.log("==========senderBalance:" + senderBalance);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });

//   it("should feesAvailable & withdrawFees StableCoin 2", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;
//     //  console.log("ammount:"+ammount)

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     //call approve
//     await usdt.connect(Receiver).approve(paydeceEscrow.address, ammount);

//     await expect(
//       paydeceEscrow.connect(other).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(owner).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Amount > feesAvailable");

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");

//     const _feesAvailable = await paydeceEscrow
//       .connect(owner)
//       .feesAvailable(usdt.address);
//     // console.log("feesAvailable:" + _feesAvailable);

//     const scBalance = await usdt.balanceOf(owner.address);

//     // console.log("scBalance:" + scBalance);
//     //withdrawFees
//     const _releaseEscrowOwner = await paydeceEscrow
//       .connect(owner)
//       .withdrawFees(usdt.address);

//     //get Balance
//     const scAfterBalance = await usdt.balanceOf(owner.address);
//     // console.log("scAfterBalance:" + scAfterBalance);

//     expect(Number(scAfterBalance)).to.equal(
//       Number(scBalance) + Number(_feesAvailable)
//     );
//   });
// });

// //CancelReceiverOwner
// describe("PaydeceEscrow StableCoin releaseEscrowOwner", function () {
//   it("should releaseEscrowOwner to Sender StableCoin 3", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Address Stable to be whitelisted");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     // await expect(
//     //   paydeceEscrow
//     //     .connect(Receiver)
//     //     .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     // ).to.be.revertedWith("Receiver approve to Escrow first");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrow("1", Sender.address, ammount, usdt.address, false, false)
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrow("1", Sender.address, ammount, usdt.address, true, true)
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrow("1")
//     ).to.be.revertedWith("Only Sender can call this");

//     //call setMarkAsPaid
//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");

//     await expect(
//       paydeceEscrow.connect(Sender).setMarkAsPaid("1")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");

//     //get Balance
//     // const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // expect(Number(scBalance)).to.equal(Number(1 * 10 ** decimals));
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     expect(Number(receiverBalance)).to.equal(Number(0));
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     expect(Number(senderBalance)).to.equal(Number(100 * 10 ** decimals));
//   });

//   it("should refund to Receiver StableCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(
//         paydeceEscrow.address,
//         ammount + _amountFeeReceiver + _amountFeeReceiver
//       );

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     //call refundReceiver Error
//     await expect(
//       paydeceEscrow.connect(other).refundSender("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call refundReceiver
//     await paydeceEscrow.connect(owner).refundSender("1");

//     //get Balance
//     const scBalance = await usdt.balanceOf(paydeceEscrow.address);
//     // console.log("==========scBalance:" + scBalance);
//     expect(Number(scBalance)).to.equal(Number(0));
//     const receiverBalance = await usdt.balanceOf(Receiver.address);
//     // console.log("==========ReceiverBalance:" + ReceiverBalance);
//     expect(Number(receiverBalance)).to.equal(
//       Number(100 * 10 ** decimals) + 1000000
//     );
//     const senderBalance = await usdt.balanceOf(Sender.address);
//     // console.log("==========SenderBalance:" + SenderBalance);
//     expect(Number(senderBalance)).to.equal(Number(0));
//   });

//   it("should feesAvailable & withdrawFees StableCoin 3", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;
//     //  console.log("ammount:"+ammount)

//     //transfer
//     await usdt.transfer(Receiver.address, ammount);

//     //call approve
//     await usdt.connect(Receiver).approve(paydeceEscrow.address, ammount);

//     await expect(
//       paydeceEscrow.connect(other).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(owner).withdrawFees(usdt.address)
//     ).to.be.revertedWith("Amount > feesAvailable");

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwner("1");

//     const _feesAvailable = await paydeceEscrow
//       .connect(owner)
//       .feesAvailable(usdt.address);
//     // console.log("feesAvailable:" + _feesAvailable);

//     const scBalance = await usdt.balanceOf(owner.address);

//     // console.log("scBalance:" + scBalance);
//     //withdrawFees
//     const _releaseEscrowOwner = await paydeceEscrow
//       .connect(owner)
//       .withdrawFees(usdt.address);

//     //get Balance
//     const scAfterBalance = await usdt.balanceOf(owner.address);
//     // console.log("scAfterBalance:" + scAfterBalance);

//     expect(Number(scAfterBalance)).to.equal(
//       Number(scBalance) + Number(_feesAvailable)
//     );
//   });


//   //Require FIATCOIN_TRANSFERED
//   it("should refund StableCoin Require FIATCOIN_TRANSFERED", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;

//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(
//         paydeceEscrow.address,
//         ammount + _amountFeeReceiver + _amountFeeReceiver
//       );

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     //call refundReceiver Error
//     await expect(
//       paydeceEscrow.connect(other).refundSender("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("1");

//     //call refundReceiver
//     await paydeceEscrow.connect(owner).refundSender("1");

    
//   });

// });

// describe("PaydeceEscrow NativeCoin", function () {
//   //NativeCoin

//   it("should create a escrow and release NativeCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     const initReceiverBalance = await ethers.provider.getBalance(Sender.address);
//     const initSenderBalance = await ethers.provider.getBalance(Receiver.address);

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await expect(
//       paydeceEscrow
//         .connect(Sender)
//         .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//           value: ammount,
//         })
//     ).to.be.revertedWith("Receiver cannot be the same as sender");

//     //call createEscrow zero
//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrowNativeCoin("2", Sender.address, 0, false, false)
//     ).to.be.revertedWith("The parameter value cannot be zero");

//     //call createEscrow Receiver zero
//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrowNativeCoin(
//           "2",
//           ethers.constants.AddressZero,
//           0,
//           false,
//           false
//         )
//     ).to.be.revertedWith("The address receiver cannot be empty");

//     //Error the ammount
//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//           value: 0,
//         })
//     ).to.be.revertedWith("Incorrect amount");

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });

//     await expect(
//       paydeceEscrow
//         .connect(Receiver)
//         .createEscrowNativeCoin("2", Sender.address, ammount, true, true, {
//           value: ammount,
//         })
//     ).to.be.revertedWith("Escrow already exists");

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Only Sender can call this");

//     await expect(
//       paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Native Coin has not been deposited");

//     await expect(
//       paydeceEscrow.connect(Receiver).setMarkAsPaid("2")
//     ).to.be.revertedWith("Only Receiver can call this");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("2");

//     //call releaseEscrow
//     await paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2");

//     //get balance SC paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("afterbalanceSC:"+afterbalanceSC.toString())
//     // 1 is expected because 1% of 100
//     // expect(Number(afterbalanceSC)).to.equal(
//     //   Number(ethers.utils.parseUnits("1", "ether"))
//     // );

//     receiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("ReceiverBalance:"+ReceiverBalance.toString())

//     // console.log("ammount:"+ammount)

//     senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())
//     // expect(Number(SenderBalance)).to.equal(Number(initSenderBalance)+Number(ethers.utils.parseUnits("99", "ether")));
//   });

//   it("should releaseEscrowOwnerNativeCoin to Sender NativeCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     let receiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("----------antes ReceiverBalance:"+ReceiverBalance.toString())

//     let senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     //get balance sc paydece
//     const prevBalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log(prevBalance.toString())

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).releaseEscrowOwnerNativeCoin("2")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call releaseEscrowOwner
//     await expect(
//       paydeceEscrow.connect(owner).releaseEscrowOwnerNativeCoin("2")
//     ).to.be.revertedWith("Native Coin has not been deposited");

//     await expect(
//       paydeceEscrow.connect(Receiver).setMarkAsPaid("2")
//     ).to.be.revertedWith("Only Receiver can call this");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("2");

//     //call releaseEscrowOwner
//     await paydeceEscrow.connect(owner).releaseEscrowOwnerNativeCoin("2");

//     //get balance sc paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     //  console.log("----------afterbalanceSC:"+afterbalanceSC.toString())
//     // expect(Number(afterbalanceSC)).to.equal(Number(0));

//     senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("----------SenderBalance:"+SenderBalance.toString())

//     receiverBalance = await ethers.provider.getBalance(Receiver.address);
//     //  console.log("----------ReceiverBalance:"+ReceiverBalance.toString())

//     // 1 is expected because 1% of 100
//     // /expect(Number(afterbalanceSC)).to.equal(Number(ethers.utils.parseUnits("1", "ether")));
//     // expect(Number(ReceiverBalance)).to.be.at.least(Number(ethers.utils.parseUnits("100", "ether")));
//   });

//   it("should refund to refundSenderNativeCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     let senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("senderBalance:"+senderBalance.toString())

//     //get balance sc paydece
//     const prevBalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log(prevBalance.toString())

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).refundSenderNativeCoin("2")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call releaseEscrow
//     await paydeceEscrow.connect(owner).refundSenderNativeCoin("2");

//     await expect(
//       paydeceEscrow.connect(owner).refundSenderNativeCoin("2")
//     ).to.be.revertedWith("Refund not approved");

//     //get balance sc paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("----------afterbalanceSC:"+afterbalanceSC.toString())
//     expect(Number(afterbalanceSC)).to.equal(Number(0));

//   });

//   //Require FIATCOIN_TRANSFERED
//   it("should refund refundSenderNativeCoin Require FIATCOIN_TRANSFERED", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     let senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     //get balance sc paydece
//     const prevBalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log(prevBalance.toString())

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });

//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(other).refundSenderNativeCoin("2")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("2");
    
//     await paydeceEscrow.connect(owner).refundSenderNativeCoin("2")
    
//     // //call releaseEscrow
//     // await paydeceEscrow.connect(owner).refundSenderNativeCoin("2");
//     // console.log("Paso2");
//     // await expect(
//     //   paydeceEscrow.connect(owner).refundSenderNativeCoin("2")
//     // ).to.be.revertedWith("Refund not approved");
//     // console.log("Paso3");
//     // //get balance sc paydece
//     // const afterbalanceSC = await ethers.provider.getBalance(
//     //   paydeceEscrow.address
//     // );
//     // // console.log("----------afterbalanceSC:"+afterbalanceSC.toString())
//     // expect(Number(afterbalanceSC)).to.equal(Number(0));

//   });

//   it("should refund to CancelSenderNative", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     let senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     //get balance sc paydece
//     const prevBalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log(prevBalance.toString())

//     await expect(
//       paydeceEscrow.connect(other).setTimeProcess("1")
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     // set TimeProcess
//     await paydeceEscrow.connect(owner).setTimeProcess("1");

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });
    
//     await paydeceEscrow.connect(owner).setTimeProcess(100000);
    
//     await expect(
//       paydeceEscrow.connect(Receiver).cancelSenderNative("2")
//     ).to.be.revertedWith("Time is still running out.");
    
//     await paydeceEscrow.connect(owner).setTimeProcess(1);
    
//     //call releaseEscrow Error
//     await expect(
//       paydeceEscrow.connect(Sender).cancelSenderNative("2")
//     ).to.be.revertedWith("Only Sender can call this");
    
//     //call releaseEscrow
//     await paydeceEscrow.connect(Receiver).cancelSenderNative("2");

//     await expect(
//       paydeceEscrow.connect(Receiver).cancelSenderNative("2")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");

//     //get balance sc paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );

//     // console.log("----------afterbalanceSC:"+afterbalanceSC.toString())
//     expect(Number(afterbalanceSC)).to.equal(Number(0));

//     // SenderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     // ReceiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("ReceiverBalance:"+ReceiverBalance.toString())

//     // 1 is expected because 1% of 100
//     // expect(Number(afterbalanceSC)).to.equal(Number(ethers.utils.parseUnits("1", "ether")));

//     // expect(Number(ReceiverBalance)).to.be.at.least(Number(ethers.utils.parseUnits("99", "ether")));
//   });

//   it("should refund to CancelReceiverNative", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     // let SenderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     //get balance sc paydece
//     const prevBalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log(prevBalance.toString())

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether
//     const ammount_fee = ethers.utils.parseUnits("100", "ether"); //1 ether

//     // console.log(ammount.toString());
//     // console.log(ammount_fee.toString());

//     const afterbalanceSC1 = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("----------afterbalanceSC1:" + afterbalanceSC1.toString());

//     await paydeceEscrow
//       .connect(Sender)
//       .createEscrowNativeCoin("3", Receiver.address, ammount, true, true, {
//         value: ammount_fee,
//       });

//     // var escrow = await paydeceEscrow.escrows("3");
//     // console.log(escrow);

//     const afterbalanceSC2 = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("----------afterbalanceSC2:" + afterbalanceSC2.toString());

//     await expect(
//       paydeceEscrow.connect(Sender).cancelReceiverNative("3")
//     ).to.be.revertedWith("Only Receiver can call this");

//     // var escrow = await paydeceEscrow.escrows(2);

//     await paydeceEscrow.connect(Receiver).cancelReceiverNative("3");

//     await expect(
//       paydeceEscrow.connect(Receiver).cancelReceiverNative("3")
//     ).to.be.revertedWith("Status must be CRYPTOS_IN_CUSTODY");


//     //get balance sc paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );

//     // console.log("----------afterbalanceSC:" + afterbalanceSC.toString());
//     expect(Number(afterbalanceSC)).to.equal(Number(0));

//     // SenderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())

//     // ReceiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("ReceiverBalance:"+ReceiverBalance.toString())

//     // 1 is expected because 1% of 100
//     // expect(Number(afterbalanceSC)).to.equal(Number(ethers.utils.parseUnits("1", "ether")));

//     // expect(Number(ReceiverBalance)).to.be.at.least(Number(ethers.utils.parseUnits("99", "ether")));
//   });

//   it("should feesAvailableNativeCoin & withdrawFeesNativeCoin NativeCoin Premium", async function () {
//     let PaydeceEscrow, paydeceEscrow;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     const initReceiverBalance = await ethers.provider.getBalance(Sender.address);
//     const initSenderBalance = await ethers.provider.getBalance(Receiver.address);

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Error OnlyOwner
//     await expect(
//       paydeceEscrow.connect(other).withdrawFeesNativeCoin()
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(owner).withdrawFeesNativeCoin()
//     ).to.be.revertedWith("Amount > feesAvailable");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, true, true, {
//         value: ammount,
//       });

//     //call releaseEscrow
//     await expect(
//       paydeceEscrow.connect(Sender).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Only Sender can call this");

//     await expect(
//       paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Native Coin has not been deposited");

//     await expect(
//       paydeceEscrow.connect(Receiver).setMarkAsPaid("2")
//     ).to.be.revertedWith("Only Receiver can call this");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("2");

//     await paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2");

//     //get balance SC paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("afterbalanceSC", Number(afterbalanceSC));

//     // console.log("afterbalanceSC:"+afterbalanceSC.toString())
//     // 1 is expected because 1% of 100
//     expect(Number(afterbalanceSC)).to.equal(Number(0));

//     receiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("ReceiverBalance:"+ReceiverBalance.toString())

//     // console.log("ammount:"+ammount)

//     senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())
//     // expect(Number(SenderBalance)).to.equal(Number(initSenderBalance)+Number(ethers.utils.parseUnits("99", "ether")));

//     const _feesAvailable = await paydeceEscrow
//       .connect(owner)
//       .feesAvailableNativeCoin();
//     // console.log("_feesAvailable", Number(_feesAvailable));

//     const scBalance = await ethers.provider.getBalance(owner.address);
//     // console.log("scBalance", Number(scBalance));

//     //withdrawFees
//     // const _releaseEscrowOwner = await paydeceEscrow
//     //   .connect(owner)
//     //   .withdrawFeesNativeCoin();

//     // const txReceipt = await _releaseEscrowOwner.wait();
//     // const effGasPrice = txReceipt.effectiveGasPrice;
//     // const txGasUsed = txReceipt.gasUsed;
//     // const gasUsedETH = effGasPrice * txGasUsed;
//     // console.debug(
//     //   "Total Gas USD: " + ethers.utils.formatEther(gasUsedETH.toString()) // exchange rate today
//     // );

//     //get Balance
//     const scAfterBalance = await ethers.provider.getBalance(owner.address);
//     // console.log("scAfterBalance:" + scAfterBalance);

//     // expect(Number(scAfterBalance)).to.equal(
//     //   Number(scBalance) + Number(_feesAvailable) - Number(gasUsedETH)
//     // );
//   });

//   it("should feesAvailableNativeCoin & withdrawFeesNativeCoin NativeCoin", async function () {
//     let PaydeceEscrow, paydeceEscrow;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();
//     // console.log(owner.address)

//     const initReceiverBalance = await ethers.provider.getBalance(Sender.address);
//     const initSenderBalance = await ethers.provider.getBalance(Receiver.address);

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //Error OnlyOwner
//     await expect(
//       paydeceEscrow.connect(other).withdrawFeesNativeCoin()
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await expect(
//       paydeceEscrow.connect(owner).withdrawFeesNativeCoin()
//     ).to.be.revertedWith("Amount > feesAvailable");

//     //call createEscrow
//     const ammount = ethers.utils.parseUnits("100", "ether"); //1 ether

//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrowNativeCoin("2", Sender.address, ammount, false, false, {
//         value: ammount,
//       });

//     //call releaseEscrow
//     await expect(
//       paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Native Coin has not been deposited");

//     await expect(
//       paydeceEscrow.connect(owner).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Only Sender can call this");

//     await expect(
//       paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2")
//     ).to.be.revertedWith("Native Coin has not been deposited");

//     await expect(
//       paydeceEscrow.connect(Receiver).setMarkAsPaid("2")
//     ).to.be.revertedWith("Only Receiver can call this");

//     await paydeceEscrow.connect(Sender).setMarkAsPaid("2");

//     await paydeceEscrow.connect(Receiver).releaseEscrowNativeCoin("2");

//     //get balance SC paydece
//     const afterbalanceSC = await ethers.provider.getBalance(
//       paydeceEscrow.address
//     );
//     // console.log("afterbalanceSC", Number(afterbalanceSC));

//     // console.log("afterbalanceSC:"+afterbalanceSC.toString())
//     // 1 is expected because 1% of 100
//     // expect(Number(afterbalanceSC)).to.equal(Number(0));

//     receiverBalance = await ethers.provider.getBalance(Receiver.address);
//     // console.log("ReceiverBalance:"+ReceiverBalance.toString())

//     // console.log("ammount:"+ammount)

//     senderBalance = await ethers.provider.getBalance(Sender.address);
//     // console.log("SenderBalance:"+SenderBalance.toString())
//     // expect(Number(SenderBalance)).to.equal(Number(initSenderBalance)+Number(ethers.utils.parseUnits("99", "ether")));

//     const _feesAvailable = await paydeceEscrow
//       .connect(owner)
//       .feesAvailableNativeCoin();
//     // console.log("_feesAvailable", Number(_feesAvailable));

//     const scBalance = await ethers.provider.getBalance(owner.address);
//     // console.log("scBalance", Number(scBalance));

//     //withdrawFees
//     const _releaseEscrowOwner = await paydeceEscrow
//       .connect(owner)
//       .withdrawFeesNativeCoin();

//     // const txReceipt = await _releaseEscrowOwner.wait();
//     // const effGasPrice = txReceipt.effectiveGasPrice;
//     // const txGasUsed = txReceipt.gasUsed;
//     // const gasUsedETH = effGasPrice * txGasUsed;
//     // console.debug(
//     //   "Total Gas USD: " + ethers.utils.formatEther(gasUsedETH.toString()) // exchange rate today
//     // );

//     //get Balance
//     const scAfterBalance = await ethers.provider.getBalance(owner.address);
//     // console.log("scAfterBalance:" + scAfterBalance);

//     // expect(Number(scAfterBalance)).to.equal(
//     //   Number(scBalance) + Number(_feesAvailable) - Number(gasUsedETH)
//     // );
//   });
// });

// describe("Contract Ownership", () => {
//   it("you should transfer Ownership", async () => {
//     let PaydeceEscrow, paydeceEscrow;
//     const [owner, newOwner,other] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     await expect(
//       paydeceEscrow.connect(other).transferOwnership(ethers.constants.AddressZero)
//     ).to.be.revertedWith("Ownable: caller is not the owner");
    
//     await expect(
//       paydeceEscrow.transferOwnership(ethers.constants.AddressZero)
//     ).to.be.revertedWith("Ownable: new owner is the zero address");

//     await paydeceEscrow.transferOwnership(newOwner.address);

//     const _owner = await paydeceEscrow.owner();
//     // console.log("_owner:" + _owner);
//     expect(_owner).to.equal(newOwner.address);
//   });

//   it("you should renounce Ownership", async () => {
//     let PaydeceEscrow, paydeceEscrow;
//     const [owner, newOwner,other] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     await expect(
//       paydeceEscrow.connect(other).renounceOwnership()
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     await paydeceEscrow.renounceOwnership();

//     const _owner = await paydeceEscrow.owner();
//     // console.log("_owner:" + _owner);
//     expect(_owner).to.equal("0x0000000000000000000000000000000000000000");
//   });

//   it("should create a escrow and get state equal 1", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses Error
//     await expect(
//       paydeceEscrow.connect(other).addStablesAddresses(usdt.address)
//     ).to.be.revertedWith("Ownable: caller is not the owner");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;
//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     //  console.log("ammount:"+ammount)

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     //call getState
//     const _state = await paydeceEscrow.getState("1");
//     //  console.log("_state:" + _state);
//     const _FundedState = 2;
//     expect(_state).to.equal(_FundedState);
//   });

//   it("should create a escrow and get value equal 1", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeSender("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;
//     const _amountFeeReceiver =
//       (ammount * (1000 * 10 ** decimals)) / (100 * 10 ** decimals) / 1000;
//     //  console.log("ammount:"+ammount)

//     //transfer
//     await usdt.transfer(Receiver.address, ammount + _amountFeeReceiver);

//     //call approve
//     await usdt
//       .connect(Receiver)
//       .approve(paydeceEscrow.address, ammount + _amountFeeReceiver);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Receiver)
//       .createEscrow("1", Sender.address, ammount, usdt.address, false, false);

//     //call getState
//     // const _value = await paydeceEscrow.getBalance("1");
//     // console.log("Paso 9");
//     // const _FundedState = 1;
//     // expect(_value).to.equal(ammount);
//   });

//   it("should create a escrow and get Escrow", async function () {
//     let PaydeceEscrow, paydeceEscrow;
//     let USDT, usdt;

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     // Deploy USDT
//     USDT = await ethers.getContractFactory("USDTToken");
//     usdt = await USDT.deploy();
//     await usdt.deployed();

//     const [owner, Sender, Receiver, other] = await ethers.getSigners();

//     // console.log("Sender:"+Sender.address);
//     // console.log("Receiver:"+Receiver.address); 

//     //Set Fee to 1%
//     // await paydeceEscrow.connect(owner).setFeeReceiver("1000");

//     //call addStablesAddresses
//     await paydeceEscrow.connect(owner).addStablesAddresses(usdt.address);

//     //Set amount
//     const decimals = await usdt.connect(Receiver).decimals();
//     const ammount = 100 * 10 ** decimals;
//     //  console.log("ammount:"+ammount)

//     //transfer
//     await usdt.transfer(Sender.address, ammount);

//     //call approve
//     await usdt.connect(Sender).approve(paydeceEscrow.address, ammount);

//     //call createEscrow
//     await paydeceEscrow
//       .connect(Sender)
//       .createEscrow("1", Receiver.address, ammount, usdt.address, false, false);

//     //call getState
//     const _escrow = await paydeceEscrow.escrows("1");
//     // console.table(_escrow);
//     // console.log("_escrow.Receiver:" + _escrow.receiver);
//     // console.log("Receiver.address:" + Receiver.address);
//     // console.log("Sender.address:" + Sender.address);
//     // console.log("_escrow.Sender:" + _escrow.sender);

//     // const _FundedState = 1;
//     expect(_escrow.receiver).to.equal(Receiver.address);
//     expect(_escrow.sender).to.equal(Sender.address);
//     expect(_escrow.value).to.equal(ammount);
//     // expect(_escrow.receiverfee).to.equal(1000);
//     expect(_escrow.senderfee).to.equal(0);
//     expect(_escrow.currency).to.equal(usdt.address);
//     expect(_escrow.status).to.equal(2);
//   });
// });

// describe("Contract Read Methods", () => {
//   it("version should be 4.1.0", async () => {
//     let PaydeceEscrow, paydeceEscrow;
//     const [owner, newOwner] = await ethers.getSigners();

//     PaydeceEscrow = await ethers.getContractFactory("PaydeceEscrow");
//     paydeceEscrow = await PaydeceEscrow.deploy();
//     await paydeceEscrow.deployed();

//     const _verion = await paydeceEscrow.version();

//     //console.log("_verion:" + _verion);
//     expect(_verion).to.equal("4.2.0");
//   });
// });